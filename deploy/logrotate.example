# nightcrew 로그 회전 — /etc/logrotate.d/nightcrew 로 복사 (데스크톱 WSL2)
# 위성이 macOS면 logrotate가 없다 — `tail -c 5M` 주기 정리나 newsyslog 사용(리눅스/VPS는 이 파일 그대로).
/home/*/nightcrew/*.log {
    weekly
    rotate 8
    compress
    missingok
    notifempty
    copytruncate
}
