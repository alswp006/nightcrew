# nightcrew 로그 회전 — /etc/logrotate.d/nightcrew 로 복사 (데스크톱 WSL2)
# M1은 macOS라 logrotate가 없다 — 주기적으로 `tail -c 5M` 정리하거나 newsyslog 사용.
/home/*/nightcrew/*.log {
    weekly
    rotate 8
    compress
    missingok
    notifempty
    copytruncate
}
